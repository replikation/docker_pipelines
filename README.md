# Docker_pipelines

## Install
### simple

* install docker via `sudo apt install`
* add docker group to ypour user
* execute `install_docker_images.sh`

### advanced

* execute both install skripts, if you are on ubuntu
* max sure before that every dependency is there
* follows the install routine from docker (adding the docker repo to your computer)

1. installs current version for ubuntu, also **adds docker group to user**
2. pulls all the dockers that you need


## Script

* run cb_docker_assembly.sh
